# CLAUDE.md

이 저장소에서 작업할 때 따를 지침입니다.

## 저장소 목적
**새 프로젝트를 시작할 때마다 적용하는 Claude 기본 셋팅(부트스트랩 템플릿)** 입니다. 나만의 커스텀 에이전트·스킬·커맨드·워크플로우를 여기서 작성·검증·버전 관리하고, 검증된 자산을 전역 `~/.claude` 또는 프로젝트별 `.claude/`로 심링크/복사해 실제 Claude Code에서 재사용합니다. 즉 여기 자산은 특정 프로젝트가 아니라 **여러 프로젝트에 걸쳐 재사용하는 기본값**입니다.

## 규칙
- **네이밍**: 모든 자산 이름은 kebab-case.
- **frontmatter**: `agents/`, `skills/`의 파일은 `name`, `description`을 반드시 채운다. `description`에는 "언제 쓰는지(트리거)"를 명확히 적는다.
- **최소 권한**: 에이전트의 `tools`는 실제 필요한 것만 나열한다.
- **배치**: 자산은 유형에 맞는 디렉터리에 둔다 — `agents/`, `skills/`, `commands/`, `workflows/`.
- **프로젝트 비종속(재사용성)**: 자산은 여러 프로젝트에 적용되므로 특정 프로젝트 전용 경로·이름·가정을 넣지 말고 **범용적으로** 작성한다. 한 프로젝트에서만 쓰는 자산은 이 템플릿이 아니라 그 프로젝트의 `.claude/`에 따로 둔다.

## 커밋
- 자산 하나 = 커밋 하나를 기본으로, 관련 변경은 함께 묶는다.
- 커밋/푸시는 사용자가 요청할 때만 수행한다.
