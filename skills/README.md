# skills/

커스텀 스킬을 두는 곳입니다. 스킬별 폴더 안에 `SKILL.md`와 보조 자료를 둡니다.

```
skills/
└── my-skill/
    ├── SKILL.md        # 트리거 조건 + 수행 절차
    └── references/     # 보조 문서·템플릿 (선택)
```

`SKILL.md` frontmatter의 `description`에 **언제 발동하는지(트리거)**를 명확히 적습니다.
